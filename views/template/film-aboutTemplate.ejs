<script type="text/template" id="film-template">
  {{each list}}
        <dd>
          <img src="{{$value.showing_pic}}" alt="">
          <strong>{{$value.showing_name}}</strong>
        </dd>
        {{/each}}     
</script>