<script type="text/template" id="film-template">
  {{each list}}
        <dd>
          <img src="{{$value.show_pic}}" alt="">
          <strong>{{$value.show_name}}</strong>
        </dd>
        {{/each}}     
</script>